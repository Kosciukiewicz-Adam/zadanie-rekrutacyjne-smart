enum QueryKeys {
    USERS = 'users',
}

enum QueryStatus {
    LOADING = 'loading',
    SUCCESS = 'success',
    ERROR = 'error',
    IDLE = 'idle',
}

enum SearchTermsKeyNames {
    USERNAME = "username",
    EMAIL = "email",
    PHONE = "phone",
    NAME = "name",
}

export { QueryKeys, QueryStatus, SearchTermsKeyNames };